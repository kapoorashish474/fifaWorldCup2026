import type { ActualResult } from './predictions';

/**
 * Update this file as real results come in during the tournament.
 * matchId must match ids in src/data/predictions.ts (e.g. "claude-r32-1").
 */
export const actualResults: ActualResult[] = [
  // { matchId: 'claude-r32-1', actualWinner: 'Mexico', actualScore: '2-0' },
];
